import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'autodeploy-ui',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
